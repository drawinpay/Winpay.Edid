<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="clr-namespace:EdidEditor.Theme.Light.Helpers">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Brushes.xaml" />
        <ResourceDictionary Source="Geometry.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <SolidColorBrush x:Key="Brush.Gray.6" Color="{StaticResource Color.Gray.6}" />

    <Style x:Key="RepeatButton.Default" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Delay" Value="500" />
        <Setter Property="Interval" Value="100" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RepeatButton.Geometry" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Delay" Value="500" />
        <Setter Property="Interval" Value="100" />
        <Setter Property="helpers:AttachedProperties.GeometrySize" Value="12" />
        <Setter Property="helpers:AttachedProperties.GeometryFill" Value="{StaticResource Brush.Gray.6}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Path
                            Width="{Binding Path=ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                            Height="{Binding Path=ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Data="{Binding Path=(helpers:AttachedProperties.Geometry), RelativeSource={RelativeSource TemplatedParent}}"
                            Fill="{Binding Path=(helpers:AttachedProperties.GeometryFill), RelativeSource={RelativeSource TemplatedParent}}"
                            Stretch="Fill" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="Style.RepeatButton.Arrow.Up"
        BasedOn="{StaticResource RepeatButton.Geometry}"
        TargetType="RepeatButton">
        <Setter Property="helpers:AttachedProperties.Geometry" Value="{StaticResource Geometry.Arrow.Up}" />
    </Style>

    <Style
        x:Key="Style.RepeatButton.Arrow.Down"
        BasedOn="{StaticResource RepeatButton.Geometry}"
        TargetType="RepeatButton">
        <Setter Property="helpers:AttachedProperties.Geometry" Value="{StaticResource Geometry.Arrow.Down}" />
    </Style>

</ResourceDictionary>