<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:EdidEditor.Theme.Light.Converters"
    xmlns:helpers="clr-namespace:EdidEditor.Theme.Light.Helpers">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Brushes.xaml" />
        <ResourceDictionary Source="Styles.TextBlock.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  默认TextBox样式  -->
    <Style x:Key="Style.TextBox.Default" TargetType="TextBox">
        <Style.Resources>
            <converters:ComputeCaretBrushConverter x:Key="ComputeCaretBrushConverter" />
        </Style.Resources>
        <Setter Property="helpers:AttachedProperties.CornerRadius" Value="0" />
        <Setter Property="MinHeight" Value="18" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="Background" Value="White" />


        <!--  文字设置  -->
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Normal}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="helpers:AttachedProperties.TextMargin" Value="4 0 4 2" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />

        <!--  描边  -->
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="helpers:AttachedProperties.BorderBrushNormal" Value="{StaticResource Brush.Border.Normal}" />
        <Setter Property="helpers:AttachedProperties.BorderBrushChecked" Value="{StaticResource Brush.Border.Normal}" />
        <Setter Property="helpers:AttachedProperties.BorderBrushWarning" Value="{StaticResource Brush.Border.Warning}" />
        <Setter Property="helpers:AttachedProperties.BorderBrushError" Value="{StaticResource Brush.Border.Error}" />


        <!--  提示文字设置  -->
        <Setter Property="helpers:AttachedProperties.HintText2Margin" Value="0 4 0 0" />
        <Setter Property="helpers:AttachedProperties.HintTextBrush" Value="{StaticResource Brush.Foreground.Hint.Normal}" />
        <Setter Property="helpers:AttachedProperties.HintText2Brush" Value="{StaticResource Brush.Foreground.Information}" />
        <Setter Property="helpers:AttachedProperties.HintText2BrushWarning" Value="{StaticResource Brush.Foreground.Warning}" />
        <Setter Property="helpers:AttachedProperties.HintText2BrushError" Value="{StaticResource Brush.Foreground.Error}" />


        <!--  光标颜色设置  -->
        <Setter Property="helpers:AttachedProperties.CaretBrushWarning" Value="{StaticResource Brush.Border.Warning}" />
        <Setter Property="helpers:AttachedProperties.CaretBrushError" Value="{StaticResource Brush.Border.Error}" />
        <Setter Property="CaretBrush">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource ComputeCaretBrushConverter}">
                    <Binding Source="{StaticResource Brush.Border.Normal}" />
                    <Binding Path="(helpers:AttachedProperties.CaretBrushWarning)" RelativeSource="{RelativeSource Mode=Self}" />
                    <Binding Path="(helpers:AttachedProperties.CaretBrushError)" RelativeSource="{RelativeSource Mode=Self}" />
                    <Binding Path="(helpers:AttachedProperties.IsWarning)" RelativeSource="{RelativeSource Mode=Self}" />
                    <Binding Path="(helpers:AttachedProperties.IsError)" RelativeSource="{RelativeSource Mode=Self}" />
                </MultiBinding>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <StackPanel>
                        <Border
                            x:Name="RootBorder"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{Binding Path=(helpers:AttachedProperties.CornerRadius), RelativeSource={RelativeSource Mode=TemplatedParent}}">
                            <Grid>
                                <ScrollViewer
                                    x:Name="PART_ContentHost"
                                    Margin="{TemplateBinding Padding}"
                                    Padding="{Binding Path=(helpers:AttachedProperties.TextMargin), RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                    VerticalAlignment="Center"
                                    FontSize="{TemplateBinding FontSize}">
                                    <ScrollViewer.Template>
                                        <ControlTemplate TargetType="ScrollViewer">
                                            <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                <Rectangle
                                                    x:Name="Corner"
                                                    Grid.Row="1"
                                                    Grid.Column="1"
                                                    Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                                                <ScrollContentPresenter
                                                    x:Name="PART_ScrollContentPresenter"
                                                    Grid.Row="0"
                                                    Grid.Column="0"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                                    CanHorizontallyScroll="False"
                                                    CanVerticallyScroll="False"
                                                    Content="{TemplateBinding Content}"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}" />
                                                <ScrollBar
                                                    x:Name="PART_VerticalScrollBar"
                                                    Grid.Row="0"
                                                    Grid.Column="1"
                                                    Cursor="Arrow"
                                                    Maximum="{TemplateBinding ScrollableHeight}"
                                                    Minimum="0"
                                                    ViewportSize="{TemplateBinding ViewportHeight}"
                                                    Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                                    Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
                                                <ScrollBar
                                                    x:Name="PART_HorizontalScrollBar"
                                                    Grid.Row="1"
                                                    Grid.Column="0"
                                                    Cursor="Arrow"
                                                    Maximum="{TemplateBinding ScrollableWidth}"
                                                    Minimum="0"
                                                    Orientation="Horizontal"
                                                    ViewportSize="{TemplateBinding ViewportWidth}"
                                                    Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                                    Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
                                            </Grid>
                                        </ControlTemplate>
                                    </ScrollViewer.Template>
                                    <ScrollViewer.Resources>
                                        <VisualBrush
                                            x:Key="Prompt"
                                            AutoLayoutContent="True"
                                            Stretch="None"
                                            TileMode="None">
                                            <VisualBrush.Visual>

                                                <!--  由于文本编辑框的文字默认有2 0的Margin，为保持提示文本位置一致添加额外的Padding="2 0"  -->
                                                <Border Padding="2,0" Background="Transparent">
                                                    <TextBlock
                                                        Padding="{Binding Path=(helpers:AttachedProperties.TextMargin), RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                                        FontSize="{Binding Path=FontSize, RelativeSource={RelativeSource AncestorType=TextBox}}"
                                                        Foreground="{Binding Path=(helpers:AttachedProperties.HintTextBrush), RelativeSource={RelativeSource AncestorType=TextBox}}"
                                                        Style="{StaticResource Style.TextBlock.Default}"
                                                        Text="{Binding Path=(helpers:AttachedProperties.HintText), RelativeSource={RelativeSource AncestorType=TextBox}}" />
                                                </Border>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </ScrollViewer.Resources>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{Binding Path=(helpers:AttachedProperties.BorderBrushChecked), RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                        </Trigger>
                        <Trigger Property="helpers:AttachedProperties.IsWarning" Value="True">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{Binding Path=(helpers:AttachedProperties.BorderBrushWarning), RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                        </Trigger>
                        <Trigger Property="helpers:AttachedProperties.IsError" Value="True">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{Binding Path=(helpers:AttachedProperties.BorderBrushError), RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource Brush.Background.Watermark.Normal}" />
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!--  默认TextBox样式  -->
    <Style BasedOn="{StaticResource Style.TextBox.Default}" TargetType="TextBox" />


    <!--  No styled TextBox, typically used for style/control assembly.  -->
    <Style x:Key="Style.TextBox.Transparent" TargetType="TextBox">
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <ScrollViewer
                        x:Name="PART_ContentHost"
                        Background="Transparent"
                        HorizontalScrollBarVisibility="Hidden"
                        VerticalScrollBarVisibility="Hidden" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!--
        A read-only TextBox style that is typically used for displaying text without allowing user edits, such as in a TextBlock-like manner.
        It is based on the transparent style to remove all default TextBox styling and behaviors, and sets IsReadOnly to true to prevent user input.
    -->
    <Style
        x:Key="Style.TextBox.TextBlock"
        BasedOn="{StaticResource Style.TextBox.Transparent}"
        TargetType="TextBox">
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
        <Setter Property="IsReadOnly" Value="True" />
    </Style>
</ResourceDictionary>