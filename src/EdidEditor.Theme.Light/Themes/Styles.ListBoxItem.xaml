<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:styles="clr-namespace:EdidEditor.Theme.Light.Helpers">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Brushes.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="Style.ListBoxItem.None" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  默认列表项（单选列表）  -->
    <Style x:Key="Style.ListBoxItem.Default" TargetType="ListBoxItem">
        <!--  文字颜色  -->
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Normal}" />
        <Setter Property="styles:AttachedProperties.ForegroundNormal" Value="{Binding Path=Foreground, RelativeSource={RelativeSource Mode=Self}}" />
        <Setter Property="styles:AttachedProperties.ForegroundChecked" Value="{Binding Path=(styles:AttachedProperties.ForegroundNormal), RelativeSource={RelativeSource Mode=Self}}" />

        <!--  背景颜色  -->
        <Setter Property="styles:AttachedProperties.BackgroundNormal" Value="{StaticResource Brush.Background.Normal}" />
        <Setter Property="styles:AttachedProperties.BackgroundHover" Value="{StaticResource Brush.Background.Hover}" />
        <Setter Property="styles:AttachedProperties.BackgroundChecked" Value="{StaticResource Brush.Background.Selected}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Grid>
                        <Border
                            x:Name="bd"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{Binding Path=(styles:AttachedProperties.BackgroundHover), RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{Binding Path=(styles:AttachedProperties.BackgroundChecked), RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>